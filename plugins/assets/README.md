# Offline model assets

`resnet18-f37072fd.pth` is the torchvision ResNet18 ImageNet-1K V1 weight
used by the Sonar-OLTR training and PLUD evaluation plugins. It is bundled so
those plugins never call the torchvision model download path at runtime.

Source URL:
https://download.pytorch.org/models/resnet18-f37072fd.pth

SHA256:
`f37072fd47e89c5e827621c5baffa7500819f7896bbacec160b1a16c560e07ec`
